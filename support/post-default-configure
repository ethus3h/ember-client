if [[ "--build-type" == "$1" ]]; then
    shift
    echo "$1" > ./support/.build-type
    shift
fi

rm ./support/.cache-build
if [[ "--cache" == "$1" ]]; then
    shift
    touch ./support/.cache-build
fi

rm -rf ./built
mkdir ./built
