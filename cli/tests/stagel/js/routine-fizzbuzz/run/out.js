async function fbSwitcher(intNum) {
    await assertIsInt(intNum); let strReturn;

    await if(await implEq(    await (await implMod(    await (    await (    )
    await (    )
    await (    )
        /* just a literal sequence of bytes. Everything should be binary safe. 0x could be used to return data, too. 0x = data, not numbers, in StageL. */
        await (        )
    /* Comment */
    await if(await implEq(    await (await implMod(    await (    await (    )
    await (    )
    await (    )
        /* Comment 2 */
        await (        )
        /* Comment 3 */
    /* Could also write it like this, with the condition on another line */
    await if(    )
await implEq(        await (await implMod(        await (        await (        )
        await (        )
        /* Comment 4 */
        await (        )
}
/* A string is an array of 8-bit bytes. */
    await return-assert(    /* the output should be non-empty. */
    await -n(    )
    await (    )
    await new(    await (    await (    )
    await until(    )
await implEq(        await (        await (        )
        await set(        await (await implAdd(        await (        await (        )
        await (        )
        await (        await (        )
