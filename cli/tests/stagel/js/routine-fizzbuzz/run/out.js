async function fbSwitcher(intNum) {
    await assertIsInt(intNum); let strReturn;

    await if(await implMod(await assertIsLiteral-n(literal-n);     )
    )
        /* just a literal sequence of bytes. Everything should be binary safe. 0x could be used to return data, too. 0x = data, not numbers, in StageL. */
        )
    /* Comment */
    await if(await implMod(await assertIsLiteral-n(literal-n);     )
    )
        /* Comment 2 */
        )
        /* Comment 3 */
    await if(    /* Could also write it like this, with the condition on another line */
    )
await implEq(await implMod(await assertIsLiteral-n(literal-n);         )
        /* Comment 4 */
        )
}
/* A string is an array of 8-bit bytes. */
    await return-assert(    /* the output should be non-empty. */
    )
    )
    await new(    )
    await until(    )
await implEq(        )
        await set(await implAdd(await assertIsInt(intCounter);         )
        )
